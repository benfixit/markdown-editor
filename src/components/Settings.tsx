import { useState } from "react";

const Settings = () => {
    return (
        <section>
        <div>
            <span>Change theme</span>
        </div>
        <textarea />
    </section>
    );
}

export default Settings;